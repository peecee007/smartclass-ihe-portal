services:
  - type: web
    name: smartclass-ihe-portal
    runtime: static
    buildCommand: npm ci && npm run build
    staticPublishPath: ./dist
    envVars:
      - key: VITE_ANTHROPIC_API_KEY
        sync: false
